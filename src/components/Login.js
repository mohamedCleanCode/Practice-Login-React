import React from "react";

const Login = () => {
  return (
    <div>
      <label>
        Username: <input type="text" placeholder="Type username" />
      </label>
      <button>Login</button>
    </div>
  );
};

export default Login;
